# Implementation Plans

Generated by the improve skill on 2026-06-11. Execute in the order below unless dependencies say otherwise. Each executor must read the target plan fully, honor its STOP conditions, and update the status row when done.

Baseline note: this repository currently has an unborn `master` branch with no commits, so the plans are stamped against the current working tree instead of a commit SHA. Use the drift checks in each plan before editing.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add characterization tests for domain data and app invariants | P1 | M | - | DONE |
| 002 | Fix hover and pin interaction state | P1 | M | 001 | DONE |
| 003 | Remediate Vite and PostCSS audit findings | P1 | S | 001 | TODO |
| 004 | Remove local and generated artifacts from repo scope | P2 | S | - | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency Notes

- 002 depends on 001 because the hover/pin change is user-facing interaction logic and should land with regression coverage.
- 003 depends on 001 by recommendation, not by code dependency: dependency updates are safer once `npm test` exists, but 003 can be executed earlier if the security update is urgent.
- 004 is independent and can be done before or after the code plans.

## Recon Summary

- App type: small React 19 + Vite + Tailwind CSS 4 TypeScript application.
- Source entry points: `src/main.tsx`, `src/App.tsx`, `src/data/socionics.ts`, `src/diagrams/AspectFunctionDiagram.tsx`.
- Package manager: npm with `package-lock.json`.
- Verified command: `npm.cmd run lint` runs `tsc --noEmit` and exited 0.
- Build command: `npm.cmd run build` exists but was not run during the audit because it writes `dist/`.
- Tests: no project test script or test files were found.
- Dependency audit: `npm.cmd audit --json` reported 5 vulnerabilities, including high-severity Vite dev-server exposure through installed `vite@6.4.1`.

## Findings Considered And Rejected

- UTF-8 mojibake in early shell output: rejected. `Get-Content -Encoding UTF8` showed README and source text decode correctly; the issue was PowerShell output encoding.
- Domain mapping structural corruption: rejected for now. An in-memory import check found no invalid aspect IDs, invalid function IDs, duplicate IDs within a view, missing decorators, missing diagrams, empty poles, or empty views.
- Hardcoded public external links: rejected. The app links to public YouTube, VK, Telegram, and Google Docs resources with `target="_blank"` plus `rel="noopener noreferrer"` where applicable.

## Not Audited

- Browser rendering, responsive layout, and visual regressions were not audited with screenshots.
- The production build was not run because this advisory mode avoided writing generated artifacts.
- Source-of-truth correctness for the socionics mappings was not independently validated against an external reference; only structural consistency was checked.
